registry=https://registry.npmmirror.com
# Electron mirrors: use ELECTRON_MIRROR / ELECTRON_BUILDER_BINARIES_MIRROR env vars.
# See scripts/electron-mirror-env.js and "npm run install:cn" for China installs.
